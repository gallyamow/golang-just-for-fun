package ratelimiter

// ConcurrencyLimiter - ограничение одновременно выполняющихся задач.
//
// Что делает:
// Ограничивает количество одновременных активных запросов.
// В отличие от Token Bucket, не учитывает скорость запросов, только текущее количество активных операций.
//
// Пример:
// Лимит: 5 одновременных запросов.
// Если 5 запросов уже обрабатываются, 6-й будет ждать или отклонён.
//
// Используется:
// Защита ресурсов, DB, сервисы с ограниченными потоками
//
// Идея:
//
// Минусы:
// Не учитывает скорость поступления запросов, поэтому не сглаживает всплески.
type ConcurrencyLimiter struct {
}
